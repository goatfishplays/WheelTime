# WheelTime
Real Time Application Launcher Styled as a Seletion Wheel


# Note For Team
- Need install cmake, be on windows, and have visual studio Desktop Development with C++
- Recomend getting the standard cmake extensions on VSCode so can just configure with `ctrl+shift+p` -> `CMake: Configure` -> `CMake: Build`